# Implementation Plans

Generated by the improve skill on 2026-07-16 (commit `0755cc8`), scoped per
the owner's directive to **UI, feel, and motion** — code-quality findings
were recorded below but deliberately not planned. Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Anchor the sidebar toggle per platform; default sidebar open on web | P1 | S | — | DONE — commit `7b97d81` on `advisor/001-anchor-sidebar-toggle` (worktree `.claude/worktrees/agent-a4a09ce6f96cfe708`); reviewed & approved, not merged |
| 002  | History heatmap: dark-mode scale, touch feel, instant day-switch, overflow guard | P1 | M | — | DONE — `advisor/002-history-heatmap-polish` (worktree `.claude/worktrees/agent-af70e60866fb1e0d5`); reviewed & approved, not merged |
| 003  | Touch-target pass on phone-visible controls | P2 | S | — | DONE — commit `7221ef8` on `advisor/003-touch-targets` (worktree `.claude/worktrees/agent-a24d90eb9ff9f66c0`); reviewed & approved, not merged |
| 004  | Motion polish: instant tab switches, modal exit animation, reduced-motion | P2 | M | — | DONE — `advisor/004-motion-polish` (worktree `.claude/worktrees/agent-ae189b7ea51c02066`); reviewed & approved, not merged |
| 005  | Dark-mode color sweep (burst flecks, media scrim, card shadow) | P3 | S | — | DONE — `advisor/005-dark-mode-color-sweep` (worktree `.claude/worktrees/agent-ac184b43615a01d91`); reviewed & approved, not merged |
| 006  | Pinned-app presence: real PNG home-screen icon + dark-aware theme-color | P1 | S | — | DONE — `advisor/006-pinned-app-presence` (worktree `.claude/worktrees/agent-a4f980a1f00d17516`); reviewed & approved, not merged |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies, but **002, 004, and 005 all add tokens to
  `src/renderer/styles.css`** — run them sequentially (or rebase between
  them) to avoid merge conflicts in the `@theme` / dark-theme blocks.
- 004's reduced-motion keyframe overrides automatically cover animations
  referenced by 002 (`fade-in`) — no coordination needed.

## Findings considered and NOT planned

Recorded so nobody re-audits them. The owner's directive for this pass was
UI/animations over code quality; the first group is real but deferred.

**Deferred (code quality — revisit if priorities change):**

- Settings page shows an infinite skeleton if `settings.get()` rejects
  (`Settings.tsx:60` — no `isError` branch). Goals renders "No goals yet"
  on a failed fetch (`Goals.tsx:30,64`). SuggestionDetail conflates fetch
  error with "not found" (`SuggestionDetail.tsx:69–79`). Fix pattern
  exists at `Integrations.tsx:88`.
- Mutation logic (status/rating/skip) duplicated and drifted between
  `ChecklistRow.tsx:45–77` (optimistic) and `SuggestionDetail.tsx:33–49,
  232–256` (invalidate-only) — the detail view visibly lags on rating
  because it lacks the optimistic path. Also two rating widgets
  (`RatingThumb` vs `RatingButton`) and two relative-date formatters
  (`History.tsx:517`, `SuggestionDetail.tsx:335`).
- Buttons hand-rolled at call sites contrary to `Button.tsx:5–9`'s own
  directive (Integrations "Try again" `:149`, BackButton
  `SuggestionDetail.tsx:207`, radio-pill pattern in Settings/GoalModal).
- Un-cleaned timers: GoalModal focus `setTimeout` (`GoalModal.tsx:40`),
  IntegrationCard 120s connect timeout not cleared on success
  (`IntegrationCard.tsx:28–44`). Harmless today.
- `as Error` casts on IPC rejections (`Integrations.tsx:90`,
  `GoalModal.tsx:146`, `IntegrationCard.tsx:47`) — a non-Error rejection
  would render a blank message.
- `GeneratingRow` renders an empty error card if `error=""`
  (`GeneratingRow.tsx:30`) — depends on callers; low value.
- Today's reflections invisible in History's day detail
  (`History.tsx:45` folds today with `reflectionsByItem: {}`) — data
  plumbing, noted in plan 002's maintenance notes.

**Rejected (checked, not defects):**

- Settings toggle knob `bg-white` and danger-button `text-white` — correct
  contrast in both themes (iOS-style knob convention); leave.
- Modal/WeatherTooltip hardcoded dark shadows — borders carry the
  elevation in dark mode; acceptable.
- `MarkdownView` XSS — react-markdown escapes raw HTML and sanitizes URLs
  by default (no `rehype-raw`); safe.
- `MediaEmbed` iframe src — constrained to known hosts via
  `src/lib/resource.ts:46–96`; safe.
- Weather tooltip being hover-only — on iOS, tap triggers the group-hover
  emulation, so it IS reachable on touch; leave.
- Sidebar `width` transition animates a layout property (perf rule says
  transform-only) — 220px panel at 200ms on desktop hardware; measured
  cost negligible, rework not worth it.
- ⌘B sidebar toggle animates a keyboard-initiated action — the same
  control is clickable; 200ms is tolerable. Optional future tweak, not a
  finding.
